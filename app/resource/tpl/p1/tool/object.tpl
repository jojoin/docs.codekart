
<h1>Tool工具箱 - object对象支持</h1>

<pre><code>var object = require_tool('object');</code></pre>

<p><dfn>javascript</dfn>对象的扩展操作。</p>

<p><big>extend(tObj,sObj,override)</big></p>

<p>深层次合并两个对象。参数<dfn>tObj</dfn>为被<b>合并修改</b>的对象，<dfn>sObj</dfn>为扩展的对象。
  <dfn>override</dfn>表示是否深层次递归合并，而不是<dfn>sObj</dfn>的属性覆盖<dfn>tObj</dfn>的。</p>

<p><big>clone(obj)</big></p>

<p>拷贝对象，并返回它的副本（一个新的对象，而不是引用）。</p>

<p><big>clone(obj)</big></p>

<p>拷贝对象，并返回它的副本（一个新的对象，而不是引用）。</p>