
<h1>前端 js 模块 - cookie</h1>

<pre><code><dfn>C.cookie</dfn></code></pre>

<h4>set(name,value,time,path)</h4>

<p><code>C.cookie.set()</code>将设置cookie。</p>

<h4>get(name)</h4>

<p>获取设置的cookie值。</p>

<h4>del(name, path)</h4>

<p>删除指定cookie值。</p>

